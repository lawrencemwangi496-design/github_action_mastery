{
  "name": "learnhub-central",
  "version": "1.0.0",
  "description": "Sample Node.js project for GitHub Actions testing",
  "main": "src/index.js",
  "scripts": {
    "build": "echo 'Building project...' && exit 0",
    "lint": "echo 'Running linter...' && exit 0",
    "test": "echo 'Running tests...' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
